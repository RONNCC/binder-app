<%= simple_form_for(@event_status_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :is_active %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
